# SPA container upstream — added to the core gateway via the compose overlay
# (docker-compose.frontend.yml). Loaded by the gateway's `include conf.d/upstreams/*.conf`.
upstream frontend { server frontend:80; }
